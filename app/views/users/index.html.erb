<% provide :title, t("users.index.index") %>
<h1><%= t("users.index.index") %></h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render @users %>
  <% end %>
</ul>

<%= will_paginate %>
